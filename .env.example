# Postgres connection for the full-stack (server) mode
DATABASE_URL="postgresql://analytics:analytics@localhost:5432/analytics?schema=public"

# Set to "true" to build the static, server-less GitHub Pages demo
# NEXT_PUBLIC_STATIC_DEMO=true
